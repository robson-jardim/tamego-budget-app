import { TrialDaysLeftPipe } from './trial-days-left.pipe';

describe('TrialDaysLeftPipe', () => {
  it('create an instance', () => {
    const pipe = new TrialDaysLeftPipe();
    expect(pipe).toBeTruthy();
  });
});
